# BLOCKERS — credentials, approvals, and inputs requiring human action

Every item here blocks a milestone. Owners are placeholders until AGP assigns names —
each needs a real owner assigned. Lead-time risk is the estimated wall-clock time from
"someone starts" to "we have working access," based on typical org/vendor turnaround.

Status legend: 🔴 not started · 🟡 in progress · 🟢 done

| # | Blocker | Needed for | Owner | Lead-time risk | Status |
|---|---------|-----------|-------|----------------|--------|
| 1 | **`kantata-tenant-grounding.md`** — the spec (SPEC.md §Non-Negotiable Constraints #7) declares this repo-root doc authoritative for AGP's Kantata instance (taxonomy custom fields, GL-coded COGS categories, rate semantics, hard/soft allocation flag, workspace_groups↔HubSpot join, dead endpoints). **It is not in the repo.** Mirror schema and financial math are built on generic Kantata semantics until it arrives; expect schema/adapter revisions when it lands. **2026-07-20: the workspace_groups↔company join and workspace custom-field values (Service Line Detail, vertical) are now pulled generically per SPEC §7 — the doc remains authoritative for GL-coded COGS categories, rate semantics, the hard/soft allocation flag, story roll-up rules, and dead endpoints.** | M1 (sync/mirror correctness), M6 (financial math) | Barry / AGP ops | Low if doc exists; **high rework risk the longer it's missing** | 🔴 |
| 2 | **`agp-org-registry-seed.json`** — parsed org chart (~60 people) the registry ingests (SPEC.md Layer 4a). **Not in the repo.** Registry work proceeds on a synthetic fixture until it arrives. | M7 (assembly/registry) | Barry / AGP ops | Low if file exists | 🔴 |
| 3 | **Kantata admin OAuth app / service token** with Subscribed Events API access. Events have ~9-day retention — once granted, polling must start promptly and stay up, and backfill should run early. Scope note: the **resourcing-engine project** (separate build — see `docs/resourcing-engine-fit.md`) additionally needs write access to resource allocations/scheduled hours for weekly reservations; request both scopes in one approval pass. **2026-07-20: API key in hand (Barry) — to be entered in Vercel as `KANTATA_API_TOKEN` per `.env.example`; adapter cut-over from fixtures is the next eng step. 2026-07-29: cut-over DONE — the live pull runs through `/api/mirror` (clients, projects, milestones, tasks, hours, posts, staff), the app is live-only with no demo workspace, and a client workspace is created pre-populated from that client's real Kantata work. The bundled fixture mirror remains only as the labelled offline fallback. Read scopes are satisfied; the resourcing engine's WRITE scopes (allocations / scheduled hours) are still outstanding and unblock nothing here.** | M1 live sync (fixtures unblock dev); resourcing engine steps 7–9 | AGP Kantata admin | 1–2 weeks (admin availability + Kantata support if OAuth app needed) | 🟢 |
| 4 | **HubSpot access** — private-app token (day one, polling fallback) and, later, a **public app** with webhook subscriptions + minimal OAuth scopes (incl. engagement content — document scope justification). **2026-07-20: private-app token in hand (Barry) — to be entered in Vercel as `HUBSPOT_PRIVATE_APP_TOKEN` per `.env.example`.** | M1 polling (private token), M5+ webhooks (public app) | AGP HubSpot admin | Private token: days. Public app: 2–4 weeks (app review + scope decisions) | 🟡 |
| 5 | **Entra ID app registration + admin consent** — Teams SSO (tab), Graph scopes for channel/member management and lazy `filesFolder` provisioning. Admin consent for Graph application permissions is often the single longest approval at nonprofits' IT partners. **2026-07-20: MS SSO confirmed as the auth (ADR 0007); server token gate built + tested, dormant behind `AUTH_REQUIRED`. START THIS NOW — copy-paste checklist for the M365 admin:** ① Entra admin center → App registrations → New registration, name "AGP Collaboration Workspace", single tenant. ② Authentication → Add platform → **Single-page application** → redirect URIs: the Vercel production URL + `https://*-preview` domains in use + `http://localhost:5199` (dev). ③ API permissions: delegated `openid`, `profile`, `email`, `User.Read` (no admin consent needed for these; Graph app-level perms come later with M3/M4 as a separate consent). ④ Send back the **Application (client) ID** and **Directory (tenant) ID** — both are public identifiers, safe to share; no secret is needed for the SPA flow. | M3 (Teams shell), M4 (provisioning); sign-in itself needs only steps ①–④ | AGP M365 admin | Steps ①–④: **days**. Graph app-level consent: 2–6 weeks | 🟡 |
| 6 | **Teams custom app upload approval** — org app catalog policy must allow sideloading/upload of the Agents Toolkit manifest, or IT must publish it. **2026-07-29 (Monday call): this workspace is the pilot for Josh's "put it in Teams as a tab" idea — if it works here, the collaboration layer of the other apps follows. Ren is scoping the setup with Jaden, who holds the admin rights. Note the first round needs NOTHING else from Entra: auth is dormant (`AUTH_REQUIRED` unset ⇒ `api/_lib/entraAuth.ts` authorizes everything), so a tab pointed at the deployed URL loads without SSO, app registration, or `access_as_user` consent. This approval is therefore the ONLY external dependency for a Teams pilot — and at 1–3 weeks it is now the longest pole on that path. Start it now.** | M3 (Teams shell) | AGP M365 admin (Jaden, with Ren) | 1–3 weeks | 🔴 |
| 7 | **Supabase project** (Postgres + pgvector) — dev instance can be free-tier and self-serve; production placement (Supabase cloud vs. self-hosted in Azure) needs an AGP infra decision. **2026-07-20: dev project created (Barry), keys in Vercel, and shared persistence is LIVE — `/api/state` stores the workspace as a versioned document in Supabase Storage (service key server-side, bucket auto-created); the app boots from it, saves debounced, polls for teammates' edits. Accepted risk until SSO: anyone with the URL shares/edits the one workspace. Postgres normalization + RLS ride the SSO wave. 2026-07-29: fixed a data-loss bug in that model — adopting the shared document replaced local state wholesale, so a workspace created seconds before a boot fetch, a 25s poll, or a 409 conflict vanished and opening it showed "Client workspace not found". Adoption now carries over items created after the remote snapshot's `savedAt`, which keeps teammates' deletions propagating (tests in `sharedState.test.ts`).** | M1 (dev DB), M8 (prod) | Eng + AGP IT | Dev: done. Prod decision: 1–2 weeks | 🟢 |
| 8 | **Anthropic API key** with production rate limits — now the top blocker: activates the LLM collaboration agents (Product Strategist, Brief Drafter) server-side. ROI Analyst runs engine-backed without it. | LLM agents (ADR 0004) | Eng | Days | 🔴 |
| 9 | **Vercel project** for dev previews (team + env vars). Repo connected 2026-07-19; previews build from `apps/tab` on every push. | M2 (preview URLs) | Eng | Hours | 🟢 |
| 10 | **Azure production environment** — subscription, resource group, container hosting for services, secrets store (Key Vault), deployment pipeline credentials. **2026-07-29: the Azure environment is in hand — procurement is no longer the long pole.** Remaining to go live on it, all owned by Ren per `docs/what-agp-needs-to-connect.md` §4: confirm the **final production URL** (it becomes the Entra redirect URI in #5), set the **build-time** vars (`VITE_ENTRA_TENANT_ID`, `VITE_ENTRA_CLIENT_ID`) so they are present when the pipeline *builds*, set the **runtime** vars (`AUTH_REQUIRED`, `ENTRA_TENANT_ID`, `ENTRA_CLIENT_ID`, `KANTATA_API_TOKEN`, the Supabase keys, any mail key), and point the deployment pipeline at this repo. We push code; Ren owns the environment. | M8 (production deploy) | AGP IT / Ren | Environment: done. Config + first deploy: days | 🟡 |
| 11 | **GivingDNA API credentials** — Phase-later; `SourceAdapter` is stubbed so the connector adds without rework. | Post-M8 | AGP product team | Unknown; not on critical path | 🔴 |
| 12 | **Teams pilot user list** — Ren asked directly on the 2026-07-29 call ("do we know which team we wanted to add a tab to?") and cannot create tabs without it. Needed: **5–8 named people with AGP emails**, and **which Teams team + channel** each tab goes in. Josh offered to pull the list. Round one can run on the live book as-is; naming a **pilot client account** is optional for round one but becomes required for round two — and that same answer tells Ren which **SharePoint site** to scope `Sites.Selected` to (#5), so it resolves two asks at once. | M3 (Teams shell) | Josh (with Cara) | Days — but it gates the work #6 unblocks | 🔴 |
| 13 | **Teams app package (ours, not AGP's)** — the repo has no `manifest.json`, no icons, no `@microsoft/teams-js` dependency, and `vercel.json` sets no `frame-ancestors` header, so the app cannot be embedded as a Teams tab yet. Small (about a day) and entirely unblocked — it needs nothing from AGP and should be built while #6 is in the approval queue. | M3 (Teams shell) | Eng | Days | 🔴 |

## Critical-path notes

- **Longest poles (revised 2026-07-29):** #10 (Azure) has landed and #3 (Kantata) is done, so
  the remaining poles are both Microsoft approvals: **#5 (Entra admin consent)** for sign-in
  and the files story, and **#6 (Teams custom app upload)** for the tab pilot. Both sit with
  AGP's M365 admin (Jaden, coordinated by Ren) and neither can be shortened by engineering —
  start them now and build #13 in parallel.
- **Live-only, as of 2026-07-29:** there is no demo workspace. The app boots empty and fills
  from the live Kantata pull; leftover seed content is purged on load and never re-seeded.
  What still runs on bundled data — and says so on screen — is the fixture mirror when
  `/api/mirror` is unreachable, and the `AGP_PEOPLE` roster behind Copilot cast suggestions
  (#2). Neither is a blocker to going live; both are labelled fallbacks.
- **#3 caution:** Kantata's 9-day event retention means the gap between "token granted" and
  "poller deployed and running" must be < 9 days or we rely on the nightly reconcile to
  recover the hole. The reconcile is built for exactly this, but don't lean on it by choice.
- **Nothing blocks the current work:** M0–M2 run entirely on fixtures by design (SPEC.md §1a).
