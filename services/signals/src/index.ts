// Signals & rules engine (M5): typed signals over the mirror, learned
// per-project/client baselines (per commercial model — SPEC domain rule 2),
// Needs-You feed ranking, dismissal learning. All actions flow through
// @agp/shared's AutonomyGate. Placeholder so the workspace typechecks from M0.
export const SERVICE = "signals" as const;
