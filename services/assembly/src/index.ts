// Auto-assembly (M7): org/capability registry ingest, playbook derivation,
// kickoff cast plans, signal-driven loop-ins with 60-second briefs.
// Capability inference is never peer-visible (SPEC hard rule; RLS enforces).
// Placeholder so the workspace typechecks end-to-end from M0.
export const SERVICE = "assembly" as const;
